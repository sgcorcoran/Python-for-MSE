# folder for testing of AI responses

## Test AI responses in this "clean" folder

- If not using a clean folder, the AI response in Cursor will use all the files and subfolders for context when responding. The students setup will be a relatively clean folder.
- To use this folder in Cursor, "Open folder/project" and select testing_F25\ so this becomes the base folder. This way parent folders are not visible.
